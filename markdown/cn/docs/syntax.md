# 配置文件中的计量单位

**Translator:** cfsego  
**Revision:** 2  
**Language:** cn

容量可以用千字节( `k` ， `K` )和兆字节( `m` ， `M` )来描述，比如“ `8k` ”，“ `1m` ”。 如果没有指定单位，容量以字节为单位。

时间可以用分钟、小时、天等来描述： 

比如，“ `1h 30m` ”，“ `1y 6M` ”。 另外，在一些指令中，描述时间可以精确到毫秒精度( `ms` )。 如果没有指定单位，时间以秒为单位。

